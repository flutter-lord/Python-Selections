num1, num2, num3, num4 = eval(input("Four numbers separated by a comma : "))

max_num = max(num1, num2, num3, num4)
min_num = min(num1, num2, num3, num4)
print(max_num, "is the maximum number and", min_num, "is the minimum number" )